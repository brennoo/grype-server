name: Main merge
on:
  push:
    branches:
      - main

permissions:
  packages: write

jobs:
  build_and_push:
    uses: ./.github/workflows/reusable-build-and-push.yml
    secrets: inherit
    with:
      image_tag: latest
      push: true
      upload: false
